# Logs

This folder holds old top-level run logs that used to clutter the repository
root. They are kept for traceability but are not part of the primary result
tables.

Canonical experiment evidence should live inside the relevant `results/...`
folder as `README.md`, `report.md`, `*.csv`, or `*.json`.
