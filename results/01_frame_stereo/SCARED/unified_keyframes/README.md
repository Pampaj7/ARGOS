# Unified SCARED keyframe ablation (fixed rectification, 45 keyframes)

GT: scatter_min_depth (R1-rotation + z-buffer projection), replaces the retired cv2.remap-based rectification used in the previous version of this report.

See leaderboard.csv for the cross-model summary.
