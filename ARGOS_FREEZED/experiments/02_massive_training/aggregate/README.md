# D2 budget-selection aggregate

**TRAINING-SCALE NO-GO.** All 1x/3x/6x means are worse than H4 on preregistered strict-common-support D2 EPE; D7 remains locked.

`per_seed_metrics.csv` and `per_budget_metrics.csv` are derived only from the validated strict D2 table. `paired_budget_comparisons.csv` is explicitly not applicable because no budget is eligible. Missing strict-table diagnostics are `null` with a reason, not inferred.
