# Experiment template

Copy this directory to a new sibling experiment ID. Fill `config.yaml` and `manifest.json`, run freeze verification and a small smoke test, then keep every generated file under this experiment's `results/`. Test mode is forbidden until a validation decision is frozen.
