# Temporal Refinement Models

Planned model implementations:

- `tiny_unet_refiner.py`: first prototype, residual disparity refiner from RGB center frame plus stacked S2M2 disparity window.
- `convgru_refiner.py`: future online recurrent refinement baseline.

No training implementation has been added yet.

